<div class="view">

  <h1 class="view-title mb-2">Citas Solicitadas</h1>

  <hr />
  <div class="card">
      <div class="card-body text-center">
            {{#each dates as |date|}}
                {{#ifDate date ../currentUser}}                  
                    <div class="card mb-3">
                      <div class="card-header">
                        <h4 class="card-title">{{date.date}} | Turno: {{date.turn}}</h4>
                        <h2>Estado: <strong>{{date.dateState}}</strong>  </h2>
                      </div>
                      <div class="card-body">
                        <h4 class="card-subtitle">Servicio: {{date.service.name}}</h4>
                        <h5 class="">{{date.type}}</h5>
                        <h4>Precio: €{{date.price}}</h4>
                        <a href="/dates/{{date.id}}/deletGuest" class="btn btn-pink col-12">Cancelar cita</a>
                      </div>
                    </div>              
                {{/ifDate}}
            {{/each}}

      </div>
    </div>

  
    
          

    </div>
  </div>

</div>